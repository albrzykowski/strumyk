net: Simple_Process
version: "1.0"

places:
  - id: p_start
    label: Start
  - id: p_end
    label: End

transitions:
  - id: t_go
    input: [p_start]
    output: [p_end]
    condition: "user['ready'] == True"
    label: "Move from start to end"
